import 'package:flutter/material.dart';

// class ThemeColors {
  const kLightBg = Color(0xFFCFD8DC);
  const klight = Color(0xFFB0BEC5);
  const kPrimary = Color(0xFF8E24AA);
  const kBlack = Colors.black;
  const kWhite = Color(0xFFFFFFFF);
  const kDarkBg = Color(0xFF37474F);
  const kDarkShadow = Color(0xFF263238);
  const kDark = Color(0xFF455A64);
// }

class AppColors {
  static const white = Color(0xFFFFFFFF);
  static const black = Colors.black;
  static const main = Color.fromRGBO(96, 32, 136, 1);
  static const lightMain = Color.fromRGBO(191, 56, 224, 1);
  static const darkMain = Color.fromRGBO(37, 6, 63, 1);
  static const grey = Color.fromRGBO(176, 154, 177, 1);
  static const shadow = Color.fromRGBO(191, 56, 224, 0.2);
}
